# Twin Times (static site)

This repository contains a static travel website located in the `mamoona/` folder. The `docs/` folder is prepared for GitHub Pages deployment.

To publish to GitHub Pages:
- Commit and push this repo to GitHub.
- On GitHub: Settings → Pages → Source: `main` branch → `/docs` folder.

The site root is `docs/index.html`.
